# 智能文档工程

## 用户故事

作为一个产出大量文档、且其中 AI 生成内容占比越来越高的经营者，我希望有一套对文档格式的定量分析方法，以人类阅读体验反向评估内容质量，以在人工逐篇审读不可行的情况下，依然能让可读性差的文档被自动识别出来。

## 场景示例

文档格式（表格、列表、正文等）可以被解析器解析出具体数字；一份文档的阅读负担因此可以量化。AI 生成的内容总体上格式偏多、偏乱——这恰好成为天然的待优化信号：格式定量指标高的文档，大概率是需要优化的文档。

与传统内容评估的差异在于评估的位置：不看文本内核讲了什么（那是叙事工程的范畴），只看承载文本的形式结构给人造成的阅读负担。两种评估互不替代——格式混乱不代表叙事差，叙事流畅也不代表阅读负担低。

## 需求详情

1. **解析层**：文档解析器把表格、列表、正文等格式要素转为可计算的数字。
2. **评估层**：以人类阅读体验为基准，建立格式定量指标与阅读负担的对应关系；指标定义需保证与人的真实感受稳定对应，这是尚未验证的核心难点。
3. **应用层**：作为内容质量指标告知哪些文档需要优化，长期与阅读负担检测融合，形成文档质量的前置过滤——与认知过滤器的「符合标准再看，不符合打回」同构。
