# platform.md

## 顶层意图

统一认知工程工具链——将平台（qtcloud-think）与实验室（project-11）从独立系统整合为正交互补的二维能力，消除维护两套心智模型和两套代码的负担。

## 中层意图

1. **格式即基础设施**：报告模板和样本必须与 gallery 标准对齐，且由程序而非手写生成。格式不是文档问题，是数据流的组成部分。

2. **无 API 也能工作**：系统在 LLM 不可用时必须产出完整可用的输出。LLM 是增强，不是必要条件。数据回退路径要有计算出来的关系、待决策和跨域信号，不能留占位符。

3. **先清理后建设**：每次引入新东西之前，先删除过时的（AGENTS.md、旧模板）、修正错误的（label_map 回退、心智模型误判）。不接受在混乱上加新东西。

## 底层意图

桥接平台与实验室的断层：CODE 的 Distill 技能（目前 TODO）是实现从笔记到 gallery YAML 自动映射的关键缺失环节，优先级最高但等待方法论定义完成后再动手。

## 优先级与风险

- 顶层意图：高优先级，高风险——整合两套系统涉及架构决策，方向一旦定错代价大
- 中层 1（格式标准）：高优先级，低风险——已基本完成
- 中层 2（无 API 可用）：高优先级，低风险——已完成
- 中层 3（先清理）：持续触发，无风险——已内化为工作习惯
- 底层意图：低优先级，低风险——等待顶层决策

## 触发条件

顶层意图的触发条件是：platform-vs-laboratory.md 的分析结论被确认为正确方向。一旦确认，下一步就是设计合并方案。
