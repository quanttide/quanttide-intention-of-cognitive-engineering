# 灵魂编译器

终局意图。其余意图都是它的组成部分。

## 用户故事

作为一个思考远多于表达速度、大量认知散落在日志与对话中的创始人，我希望有一台「灵魂编译器」——从混乱的原始数据出发，经过世界观层（提取心智模型、图式、情境、意图）与价值观层（过滤：只留高密度、意图驱动的信息），输出结构化的知识与可执行的业务动作，以让认知像代码一样可以被编译、被执行、被复用。

## 场景示例

过去用计算机处理的是「格式」；认知工程处理的是「语义和逻辑」。同一段混乱的对话，普通工具提取出「摘要、待办、关键人」，而认知工程提取出「情境中催生了什么意图、意图背后动用了什么心智模型、模型是否可以沉淀为图式」。编译器的输入形态可以无限多样——招聘数据、工作手册、短剧结构、会议记录——但流水线始终是同一条：结构化提取，价值观过滤，结构化输出。

## 需求详情

编译器分两层，缺一不可：

1. **世界观层**：划定认知的边界与结构，让 AI 知道世界由哪些基本要素构成、要素之间有哪几种关系。没有它，AI 面对混乱输入只能产出平庸的摘要。
2. **价值观层**：确立评估与选择的标尺，让 AI 知道什么是好——高密度优于完整度，意图优先于事实，结构化优于流畅度。没有它，AI 的输出是讨好与平庸。

当前进展与路径见 `data/roadmap/index.md`（M1 实验室集成 → M2 价值观编码 → M3 端到端流水线 → M4 多域泛化）。
